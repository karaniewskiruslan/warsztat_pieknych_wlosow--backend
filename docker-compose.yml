services:
  web:
    build: .
    ports: ["5000:5000"]
    environment:
      MONGO_URL: ${MONGO_URL}
      JWT_SECRET: ${JWT_SECRET}
      CLIENT_URL: ${CLIENT_URL}
    volumes:
      - img_data:/app/images
    restart: unless-stopped
volumes:
  img_data:
